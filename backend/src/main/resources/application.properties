# Spring Boot Application Configuration
spring.application.name=dora-bank
server.port=8080

# MongoDB Configuration (will be overridden by environment variables in production)
spring.data.mongodb.uri=mongodb+srv://dorarakesh8_db_user:mFfplG05VnmB2fh0@cluster0.wn9ybwl.mongodb.net/dorabank?retryWrites=true&w=majority&ssl=false

# JWT Configuration (will be overridden by environment variables in production)
jwt.secret=${JWT_SECRET:YourSuperSecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLongForSecurity}
jwt.expiration=86400000

# CORS Configuration (will be overridden by environment variables in production)
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8080}
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*

# Logging Configuration
logging.level.com.dorabank=DEBUG
logging.level.org.springframework.security=DEBUG

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
